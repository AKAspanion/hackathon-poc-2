# Database connection URL for PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
#
# Local (macOS Homebrew; if "role postgres does not exist", use your username):
#   DATABASE_URL=postgresql://YOUR_MAC_USERNAME@localhost:5432/shipping_agent
#
# DBeaver / custom host (use the host, port, user and password from your connection):
#   DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/shipping_agent
#   DATABASE_URL=postgresql://USER:PASSWORD@dbbrever:5432/shipping_agent
#
# Create the database first (in DBeaver or: createdb shipping_agent)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shipping_agent

# Optional: FastAPI app settings
APP_NAME=Shipping Risk Intelligence Service
APP_ENV=local
OPENAI_API_KEY=sandlogic
OPENAI_BASE_URL=http://45.194.2.204:3535/v1
